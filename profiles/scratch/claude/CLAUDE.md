- **cvm**: gestor de perfiles de config del harness, tipo `nvm` (`use`/`ls`/`off`/`push`).

## Skills

| Skill | Que hace |
|---|---|
| `/issue` | Crea un issue de GitHub investigando antes; profundidad (`--depth`) y umbral de ambiguedad (`--ambiguity`) configurables; `--light` = anotacion rapida sin preguntas. |
